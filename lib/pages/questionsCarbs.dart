import 'package:flutter/material.dart';
import 'package:recriacao_devoraquiz/questionscarbo/carbo1.dart';

class carb_screen extends StatelessWidget {
  const carb_screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: CARBO1(), // ignore: prefer_const_constructors
    );
  }
}
